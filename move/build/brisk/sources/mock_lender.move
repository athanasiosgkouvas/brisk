/// Testnet lender behind the adapter interface: deterministic, fast-forwardable
/// yield accrual so the Save flow is fully demonstrable without a mainnet
/// money market. Swapped for a real Suilend/Scallop adapter on mainnet.
///
/// Implemented in Phase 3.
module brisk::mock_lender;
